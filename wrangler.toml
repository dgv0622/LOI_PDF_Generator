name = "loi-pdf-generator"
compatibility_date = "2024-01-01"
pages_build_output_dir = "frontend/build"

# Cloudflare Pages configuration
# Functions will be automatically picked up from /functions directory
# Note: Build must be run separately via "npm run build" before deploying
