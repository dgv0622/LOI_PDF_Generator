{
  "name": "loi-pdf-generator",
  "version": "1.0.0",
  "description": "Letter of Intent PDF Generator for Cloudflare",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy": "npm run build && wrangler pages deploy frontend/build --project-name=loi-pdf-generator",
    "cf-typegen": "wrangler types",
    "install-all": "cd frontend && npm install --legacy-peer-deps && cd ../functions && npm install && cd .. && npm install"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
